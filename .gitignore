visual-test/
frontend/node_modules/
node_modules/
frontend/dist/
cron/dist/
cron/legacy-pets-export.json
.astro/
frontend/.astro/

# Secrets - never commit real credentials
.azure-secrets.txt
*.secrets.txt
.env
.env.*
!.env.example

# Donor PII must stay server-side (api/data). Never bake into Astro src.
frontend/src/data/donor_database.json

# Operational PII / financial dumps — keep out of new commits.
# api/data/donor_database.json remains deferred for removal from tracking:
# /api/financials still loadJsonOptional()s it at runtime in production deploys.
# Do not force-push history rewrite; migrate to private artifact store first.
api/data/donor_database.json
scratch/qbo_named_gifts*.json
scratch/*donor*.json


FROM directus/directus:11.5.0

# Directus 11 includes native MySQL2 and local storage drivers.
# Azure Blob driver can be installed into extensions directory when needed.

EXPOSE 8055
CMD ["directus", "start"]
